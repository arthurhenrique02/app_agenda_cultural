# Ralph Progress Log
Started: Fri Apr 10 09:23:32 PM -03 2026
---

## 2026-04-11 - US-013
- What was implemented: `app/seed.py` with async `seed_admin(session)` function that creates `admin@agendacultural.com` with role=admin; idempotent (skips if user already exists); password read from `settings.admin_default_password` (env var ADMIN_DEFAULT_PASSWORD); also exposes `__main__` entry point for CLI usage
- Files changed:
  - `backend/app/seed.py` - new seed module with `seed_admin()` async function
  - `backend/tests/test_seed.py` - 5 tests covering creation, password hashing, idempotency, env var password, and name field
- **Learnings for future iterations:**
  - Seed function follows the pattern: check-if-exists → create → promote. Use `get_user_by_email` to check idempotency.
  - `create_user` uses the default role `user`; to set `admin`, update `user.role` after creation and `await session.commit()` again.
  - `monkeypatch.setattr(seed_module.settings, "admin_default_password", "...")` works to override settings in tests since `settings` is a module-level singleton.
  - The seed module can be run directly: `uv run python -m app.seed` (uses `if __name__ == "__main__"` block).
---

## 2026-04-11 - US-012
- What was implemented: GET /api/users/me (already existed from US-011) + PUT /api/users/me for updating name/email; duplicate email check returns 400; 8 tests covering GET, PUT, auth required, partial update, no password hash exposure, duplicate email
- Files changed:
  - `backend/app/schemas/user.py` - added `UserUpdateRequest` schema (name/email both optional)
  - `backend/app/repositories/user.py` - added `update_user` function
  - `backend/app/services/user.py` - new module with `update_profile` service function
  - `backend/app/routers/users.py` - added `PUT /api/users/me` route
  - `backend/tests/test_profile.py` - 8 tests (TDD red→green)
- **Learnings for future iterations:**
  - `app/services/user.py` is separate from `app/services/auth.py` — user profile logic lives in the user service, auth-specific logic stays in auth service
  - `update_user` repository accepts optional name/email — only updates fields that are not None
  - Duplicate email check compares against existing user but skips if email unchanged (avoids false positive when user re-submits same email)
---

## 2026-04-11 - US-011
- What was implemented: `get_current_user` FastAPI dependency in `app/dependencies/auth.py`; extracts Bearer token, decodes JWT, validates `token_type == "access"`, looks up user by `sub` claim, returns User or raises 401; added `GET /api/users/me` route as a demo/test endpoint; 6 tests covering all 401 scenarios and successful authenticated access
- Files changed:
  - `backend/app/dependencies/auth.py` - new `get_current_user` dependency using `HTTPBearer` + `decode_token`
  - `backend/app/routers/users.py` - new router with `GET /api/users/me` protected endpoint
  - `backend/app/main.py` - registered users router
  - `backend/tests/test_auth_dependency.py` - 6 tests (TDD red→green)
- **Learnings for future iterations:**
  - Use `HTTPBearer(auto_error=False)` so the dependency returns `None` instead of auto-raising when no header is present (allows custom 401 error messages)
  - `HTTPAuthorizationCredentials | None` type annotation handles the optional case from `auto_error=False`
  - The `get_current_user` dependency can be used in any route: `current_user: User = Depends(get_current_user)`
  - stdlib `jwt` import must come before third-party imports in ruff's I001 ordering
---

## 2026-04-11 - US-010
- What was implemented: POST /api/auth/refresh endpoint; accepts `refresh_token` in body; validates JWT, checks `token_type == "refresh"`, looks up user by id, returns new access token; 7 tests covering success, invalid token, expired token, access-token-as-refresh, nonexistent user, missing body
- Files changed:
  - `backend/app/security/jwt.py` - added `decode_token` function
  - `backend/app/repositories/user.py` - added `get_user_by_id` function
  - `backend/app/schemas/auth.py` - added `RefreshRequest` and `AccessTokenResponse` schemas
  - `backend/app/services/auth.py` - added `refresh_access_token` service function
  - `backend/app/routers/auth.py` - added POST /api/auth/refresh route
  - `backend/tests/test_auth_refresh.py` - 7 tests (TDD red→green)
- **Learnings for future iterations:**
  - `decode_token` wraps `jwt.decode` — catch `jwt.PyJWTError` (base class) for both invalid signature and expiration errors
  - Always check `token_type` claim to distinguish access vs refresh tokens
  - `get_user_by_id` is a necessary repository function for token-based auth flows
---

## Codebase Patterns
- Use `StrEnum` (from `enum` stdlib) instead of `(str, enum.Enum)` — ruff UP042 enforces this.
- Alembic migrations cannot be autogenerated without a live DB. Create them manually when PostgreSQL is unavailable; offline SQL generation still works for testing.
- Migration files need blank line between stdlib imports and third-party imports (ruff I001); run `ruff check --fix` after writing migrations.

- Backend uses `uv` as Python package manager (not pip/poetry). Run `uv run pytest`, `uv run ruff check .`, `uv run black .`.
- pytest-asyncio is configured with `asyncio_mode = "auto"` in pyproject.toml - no need for explicit `@pytest.mark.asyncio` decorator (though it still works).
- Use `ASGITransport` from httpx to test FastAPI endpoints without starting a real server.
- Dev dependencies go in `[dependency-groups] dev = [...]` section (uv's format), not `[project.optional-dependencies]`.
- Use `aiosqlite` (dev dep) with `sqlite+aiosqlite:///:memory:` for in-memory async DB tests — avoids needing a real Postgres instance in unit/api tests.
- SQLAlchemy engine masks passwords in `str(engine.url)`; use `engine.url.render_as_string(hide_password=False)` to compare with the raw settings URL.
- Override `db_module.AsyncSessionLocal` in tests to swap the session factory without touching the app-level `get_db` dependency.
- Alembic env.py uses `asyncio.run(run_async_migrations())` + `async_engine_from_config` for online mode; offline mode is sync and works for SQL generation tests.
- Test Alembic offline SQL generation by running subprocess `alembic upgrade head --sql` with a dummy DATABASE_URL (no real DB needed).
- All model classes must inherit from `app.models.base.Base` (DeclarativeBase); import `app.models` in env.py for autogenerate to detect all tables.
- Shared test fixtures live in `backend/tests/conftest.py`. Use `db_session` for an isolated AsyncSession (SQLite in-memory) and `async_client` for an httpx client wired to the FastAPI app with DB overridden.
- The `async_client` fixture internally overrides `db_module.AsyncSessionLocal` to redirect all DB operations to the test SQLite engine.
- `get_db` in `dependencies/database.py` must use `import app.database as db_module` and call `db_module.AsyncSessionLocal()` — NOT `from app.database import AsyncSessionLocal` — so test fixtures can swap the factory via module attribute.
- Use `async_client_with_tables` fixture (also in conftest.py) for tests that need actual ORM models: it runs `Base.metadata.create_all` on the test SQLite engine before yielding the client.
- `pydantic[email]` (email-validator) must be installed for `EmailStr` to work: `uv add "pydantic[email]"`.
- Category seeding should load existing category names once (`select(Category.name)`) and insert only missing names before a single commit.
- For collection endpoints that must answer on a path without trailing slash (e.g., `/api/categories`), declare router path as `""` instead of `"/"` to avoid 307 redirects in API tests.
- Keep SQLAlchemy StrEnum values and Alembic enum literals strictly synchronized for the same column (e.g., event statuses) to avoid runtime/migration drift.

---

## 2026-04-11 - US-001
- What was implemented: FastAPI backend skeleton with health check endpoint (`GET /health`), pytest test harness, quality tools (ruff, black) configured in pyproject.toml
- Files changed:
  - `backend/pyproject.toml` - project config with all dependencies and tool config
  - `backend/app/__init__.py` - package init
  - `backend/app/main.py` - FastAPI app with health route
  - `backend/tests/__init__.py` - test package init
  - `backend/tests/test_health.py` - health check test (TDD red→green cycle)
  - `backend/.gitignore` - excludes pycache, .venv, etc.
  - `backend/uv.lock` - lockfile
- **Learnings for future iterations:**
  - uv creates `[dependency-groups]` section (not `[project.optional-dependencies]`) for dev deps
  - pytest-asyncio 1.3.0 uses `asyncio_mode = "auto"` config key
  - httpx `ASGITransport` is the correct way to create in-process test clients for FastAPI
  - Python version pinned to 3.12 via `.python-version` file
---

## 2026-04-11 - US-003
- What was implemented: Async SQLAlchemy engine and session factory in `app/database.py`; FastAPI `get_db` async generator dependency in `app/dependencies/database.py`; tests verifying engine type, session factory type, URL match, and live dependency injection via test SQLite DB
- Files changed:
  - `backend/app/database.py` - `create_async_engine` + `async_sessionmaker` using settings.database_url
  - `backend/app/dependencies/__init__.py` - package init
  - `backend/app/dependencies/database.py` - `get_db` async generator yielding `AsyncSession`
  - `backend/tests/test_database.py` - 5 tests covering module exports, URL, and dependency behavior
  - `backend/pyproject.toml` / `backend/uv.lock` - added `aiosqlite` dev dependency for in-memory test DB
- **Learnings for future iterations:**
  - `engine.url.render_as_string(hide_password=False)` needed to compare URL — plain `str()` masks the password
  - Swap `db_module.AsyncSessionLocal` in tests to redirect `get_db` to a test SQLite engine without patching FastAPI's DI
  - `aiosqlite` must be installed as a dev dep before using `sqlite+aiosqlite:///:memory:` URLs in tests
---

## 2026-04-11 - US-004
- What was implemented: Alembic initialized with async-compatible env.py; `app/models/base.py` with `DeclarativeBase`; baseline migration created (empty — no tables yet); 7 tests verifying config, migration existence, offline SQL generation, and imports
- Files changed:
  - `backend/alembic.ini` - Alembic config with script_location and logging
  - `backend/alembic/env.py` - async-aware env.py using `asyncio.run()` + `async_engine_from_config`
  - `backend/alembic/script.py.mako` - migration template
  - `backend/alembic/versions/bcbad57cb74f_baseline.py` - empty baseline migration
  - `backend/app/models/__init__.py` - package init re-exporting Base
  - `backend/app/models/base.py` - `DeclarativeBase` subclass
  - `backend/tests/test_migrations.py` - 7 tests for migration setup
- **Learnings for future iterations:**
  - `asyncio.run()` in env.py cannot be called inside an existing event loop (will fail in pytest-asyncio). Test Alembic via subprocess or offline mode only.
  - Offline mode (`alembic upgrade head --sql`) does not need asyncio — it's purely sync and works in any environment.
  - Always import all model modules in env.py before referencing `Base.metadata` so autogenerate detects all tables.
  - Baseline migration is intentionally empty — it just creates the alembic_version table on first `upgrade head`.
---

## 2026-04-11 - US-005
- What was implemented: Shared pytest fixtures in `conftest.py` providing `db_session` (isolated in-memory AsyncSession) and `async_client` (httpx AsyncClient wired to FastAPI app with DB override); 5 tests in `test_fixtures.py` verifying both fixtures
- Files changed:
  - `backend/tests/conftest.py` - `db_session` and `async_client` fixtures
  - `backend/tests/test_fixtures.py` - 5 tests using the fixtures (TDD red→green)
- **Learnings for future iterations:**
  - `conftest.py` fixtures are auto-discovered by pytest — no import needed in test files
  - `db_session` creates a fresh `sqlite+aiosqlite:///:memory:` engine per test for isolation
  - `async_client` overrides `db_module.AsyncSessionLocal` so the app's `get_db` dependency uses the test engine
  - Always restore `db_module.AsyncSessionLocal` in a `finally` block to avoid cross-test contamination
---

## 2026-04-11 - US-002
- What was implemented: Centralized pydantic-settings config module with env var overrides, `.env.example` with documented defaults, and tests
- Files changed:
  - `backend/app/config.py` - Settings class with all required env vars and defaults
  - `backend/.env.example` - documented env var reference
  - `backend/tests/test_config.py` - tests for defaults and env var overrides
- **Learnings for future iterations:**
  - `pydantic-settings` is already in project dependencies; use `BaseSettings` from `pydantic_settings`
  - Settings class uses `model_config = SettingsConfigDict(...)` (pydantic v2 style, not `class Config:`)
  - `extra="ignore"` prevents errors from unknown env vars
  - `case_sensitive=False` allows env vars in any case
  - black reformats long string defaults with parentheses — let black handle formatting
---

## 2026-04-11 - US-006
- What was implemented: User SQLAlchemy model with id, name, email, hashed_password, role (StrEnum: user/admin), is_active (default True), created_at (server default now()); unique index on email; manual Alembic migration a1b2c3d4e5f6_add_users_table; models/__init__.py updated to export User/UserRole
- Files changed:
  - `backend/app/models/user.py` - User model + UserRole StrEnum
  - `backend/app/models/__init__.py` - added User, UserRole exports
  - `backend/alembic/versions/a1b2c3d4e5f6_add_users_table.py` - migration creating users table with enum type and unique email index
  - `backend/tests/test_user_model.py` - 8 tests covering fields, defaults, constraints, roles
- **Learnings for future iterations:**
  - Use `StrEnum` from stdlib `enum` module — ruff UP042 flags `(str, enum.Enum)` inheritance
  - Alembic autogenerate requires a live PostgreSQL. Without one, write migrations manually
  - SQLAlchemy Enum type with `values_callable` ensures enum values (not names) are stored in DB
  - `server_default` must be a string literal for simple values; `func.now()` works for timestamps
  - Always run `ruff check --fix` + `black .` before committing to catch import order (I001) issues
---

## 2026-04-11 - US-007
- What was implemented: `app/security/password.py` module with `hash_password` and `verify_password` using argon2-cffi's `PasswordHasher`; 7 tests covering hash uniqueness, format check, correct/wrong verification, empty password
- Files changed:
  - `backend/app/security/__init__.py` - new security package
  - `backend/app/security/password.py` - `hash_password` + `verify_password`
  - `backend/tests/test_password.py` - 7 tests (TDD red→green)
- **Learnings for future iterations:**
  - `argon2-cffi` is already in project deps; use `from argon2 import PasswordHasher` and `from argon2.exceptions import VerifyMismatchError`
  - `PasswordHasher().verify(hashed, plain)` raises `VerifyMismatchError` on mismatch — catch it and return False
  - Each `hash()` call produces a unique salt, so the same password hashes differently each time (expected)
  - Hash output starts with `$argon2` — use this to verify the correct algorithm is being used
---

## 2026-04-11 - US-008
- What was implemented: POST /api/auth/register endpoint; user registration with duplicate email check (400); response excludes hashed_password; created layers: schemas/auth.py, schemas/user.py, repositories/user.py, services/auth.py, routers/auth.py; router registered in main.py
- Files changed:
  - `backend/app/schemas/auth.py` - RegisterRequest schema (name, email, password)
  - `backend/app/schemas/user.py` - UserResponse schema (excludes hashed_password)
  - `backend/app/repositories/user.py` - get_user_by_email, create_user
  - `backend/app/services/auth.py` - register_user with duplicate email check
  - `backend/app/routers/auth.py` - POST /api/auth/register route
  - `backend/app/main.py` - include auth_router
  - `backend/app/dependencies/database.py` - fixed to use module-level attribute lookup
  - `backend/tests/conftest.py` - added async_client_with_tables fixture
  - `backend/tests/test_auth_register.py` - 7 tests (TDD red→green)
  - `backend/pyproject.toml` / `backend/uv.lock` - added pydantic[email] dep
- **Learnings for future iterations:**
  - `get_db` must reference `db_module.AsyncSessionLocal` via module import, not `from ... import AsyncSessionLocal`, so test fixtures can override it
  - `async_client_with_tables` fixture needed when tests require ORM tables (runs create_all on SQLite in-memory)
  - `pydantic[email]` (email-validator) required for EmailStr field type
  - Layered structure: router calls service, service calls repository — no business logic in routers
---

## 2026-04-11 - US-009
- What was implemented: POST /api/auth/login endpoint returning JWT access token (15 min) and refresh token (7 days); 401 on invalid credentials; new JWT utility module in app/security/jwt.py; LoginRequest and TokenResponse schemas; login_user service function
- Files changed:
  - `backend/app/security/jwt.py` - new module with `create_access_token` and `create_refresh_token` using PyJWT + HS256
  - `backend/app/schemas/auth.py` - added `LoginRequest` and `TokenResponse` schemas
  - `backend/app/services/auth.py` - added `login_user` function with credential validation
  - `backend/app/routers/auth.py` - added `POST /api/auth/login` route
  - `backend/tests/test_auth_login.py` - 7 tests (TDD red→green)
- **Learnings for future iterations:**
  - PyJWT is `import jwt` (not `import pyjwt`) — use `jwt.encode(payload, key, algorithm=ALGORITHM)`
  - Use `datetime.now(UTC)` (Python 3.11+ alias) — ruff UP017 flags `timezone.utc`; auto-fixable with `ruff check --fix`
  - JWT `sub` claim should be a string — use `str(user.id)` not the int directly
  - Token type claim (`token_type: "access"/"refresh"`) lets downstream code distinguish access from refresh tokens
  - Test helper function `_login_body(user)` avoids line-too-long (E501) when building repeated request dicts
---

  ## 2026-04-10 - US-014
  - What was implemented: Added `Category` model and Alembic migration for `categories` table (unique indexed `name`); implemented `seed_default_categories(session)` in `app/seed.py` with idempotent behavior and wired it into seed CLI flow; added model and seed tests for default category insertion and duplicate prevention
  - Files changed:
    - `backend/app/models/category.py` - new Category ORM model
    - `backend/app/models/__init__.py` - exports Category
    - `backend/alembic/versions/c3d4e5f6a7b8_add_categories_table.py` - migration for categories table
    - `backend/app/seed.py` - added `DEFAULT_CATEGORIES` and `seed_default_categories`
    - `backend/tests/test_category_model.py` - model tests for fields, table name, create, unique constraint
    - `backend/tests/test_seed.py` - seed tests for category defaults and idempotency
    - `scripts/ralph/prd.json` - marked US-014 as passed
  - **Learnings for future iterations:**
    - Keep the category names stable and canonical in seeds/tests: `show`, `exposicao`, `peca`, `festival`, `outro`.
    - Reuse `seed_default_categories` in setup flows instead of hardcoding category inserts in tests or scripts.
  ---

## 2026-04-11 - US-015
- What was implemented: Added public `GET /api/categories` endpoint with layered architecture (router → service → repository); returns full category list without authentication and supports empty-list response.
- Files changed:
  - `backend/app/schemas/category.py` - new `CategoryResponse` schema
  - `backend/app/repositories/category.py` - added `list_categories` query ordered by name
  - `backend/app/services/category.py` - added category listing business service
  - `backend/app/routers/categories.py` - added public categories route
  - `backend/app/main.py` - registered categories router
  - `backend/tests/test_categories.py` - added endpoint tests (public list and empty list)
  - `scripts/ralph/prd.json` - marked US-015 as passed
- **Learnings for future iterations:**
  - For endpoint tests that need seeded data with `async_client_with_tables`, insert rows through `db_module.AsyncSessionLocal()` while the fixture override is active.
  - Keep categories endpoint public (no auth dependency) and return consistent model serialization via `model_validate` from ORM objects.
---

## 2026-04-11 - US-016
- What was implemented: Added `Event` model with full lifecycle/status fields, moderation fields (`reviewed_by`, `reviewed_at`), and required indexes (`status`, `date`, `category_id`, `created_by`); created Alembic migration to persist the events table with FKs to users/categories and status enum.
- Files changed:
  - `backend/app/models/event.py` - new `Event` model and `EventStatus` StrEnum
  - `backend/app/models/__init__.py` - exports `Event` and `EventStatus`
  - `backend/alembic/versions/d4e5f6a7b8c9_add_events_table.py` - migration creating events table and required indexes
  - `backend/tests/test_event_model.py` - model tests for fields, enum values, defaults, review metadata, and indexes
  - `backend/tests/test_event_migration.py` - migration tests for file existence and offline SQL index/table generation
  - `scripts/ralph/prd.json` - marked US-016 as passed
- **Learnings for future iterations:**
  - For cross-cutting enum fields, validate both ORM model metadata and offline Alembic SQL to ensure schema + migration alignment.
  - Add index checks at model level (`Event.__table__.indexes`) to catch missing index declarations early in TDD.
---
